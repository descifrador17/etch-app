# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`etch-app` — an AI-generated flash cards app (see `README.md`).

## Current state

The repository is currently a blank slate: a single initial commit containing only `README.md`. There is no source code, build tooling, dependency manifest, or tests yet.

As the project takes shape, update this file with:
- **Commands** — how to build, run, lint, and test (including running a single test) once tooling is chosen.
- **Architecture** — the big-picture structure (e.g. how flash-card generation, the AI integration, storage, and UI fit together) once it spans multiple files.
